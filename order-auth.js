(function(){const originalFetch=window.fetch;window.fetch=async function(input,init={}){const url=typeof input==='string'?input:(input&&input.url)||'';if(url.includes('/functions/v1/create-order')){try{const session=await tbSession();if(session){init={...init,headers:{...(init.headers||{}),Authorization:`Bearer ${session.access_token}`}}}}catch(e){console.warn('Customer session unavailable',e)}}return originalFetch.call(this,input,init)}})();
